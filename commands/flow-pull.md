---
description: Sync the Flow board - your assigned + mentioned tasks and recent activity
argument-hint: "[--focus] [--assignee=all]"
allowed-tools: Bash(flow pull:*), Bash(flow-pull:*)
---

Run `flow pull $ARGUMENTS` to fetch the current board state, then give me a short summary: what's assigned to me, any mentions or handoffs waiting, and what I should pick up next. Do this at the start of a work session.
